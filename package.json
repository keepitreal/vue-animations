{
  "name": "vue-animate",
  "version": "1.0.0",
  "description": "Simple animations directives and components for use with VueJS",
  "main": "index.js",
  "scripts": {
    "build": "browserify animations/demo.js > dist/bundle.js -t [ babelify --presets [ es2015 ] ] -t [ stringify --extensions [ .html ] ]",
    "start": "npm run watch:js",
    "watch:js": "watch 'npm run build' animations/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Darion Welch",
  "license": "MIT",
  "dependencies": {
    "vue": "^1.0.24"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "stringify": "^5.1.0",
    "watch": "^0.18.0"
  }
}
